{
  "name": "collectio",
  "private": true,
  "scripts": {
    "dev:capacitor": "pnpm --filter @collectio/capacitor-app dev",
    "build:capacitor": "pnpm --filter @collectio/capacitor-app build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
